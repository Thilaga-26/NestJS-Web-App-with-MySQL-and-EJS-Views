<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Student</title>
</head>
<body>
  <h1 style="color:red">Edit Student</h1>
  <table>
    <tbody>
      <form action="/students/<%= student.id %>" method="POST">
        <tr>
          <td><label for="name">Name:</label></td>
          <td><input type="text" id="name" name="name" value="<%= student.name %>" required></td>
        </tr>
        <tr>
          <td><label for="dept">Department:</label></td>
          <td><input type="text" id="dept" name="dept" value="<%= student.dept %>" required></td>
        </tr>
        <tr>
          <td><label for="dob">Date of Birth:</label></td>
          <td><input type="date" id="dob" name="dob" value="<%= student.dob %>" required></td>
        </tr>
        <tr>
          <td><label for="gender">Gender:</label></td>
          <td>
            <select id="gender" name="gender" required>
              <option value="Male" <%= student.gender === 'Male' ? 'selected' : '' %>>Male</option>
              <option value="Female" <%= student.gender === 'Female' ? 'selected' : '' %>>Female</option>
              <option value="Other" <%= student.gender === 'Other' ? 'selected' : '' %>>Other</option>
            </select>
          </td>
        </tr>
        <tr>
          <td><label for="address">Address:</label></td>
          <td><input type="text" id="address" name="address" value="<%= student.address %>" required></td>
        </tr>
        <tr>
          <td><label for="city">City:</label></td>
          <td><input type="text" id="city" name="city" value="<%= student.city %>" required></td>
        </tr>
        <tr>
          <td><label for="contactNo">Contact Number:</label></td>
          <td><input type="text" id="contactNo" name="contactNo" value="<%= student.contactNo %>" required></td>
        </tr>
        <tr>
          <td><label for="email">Email:</label></td>
          <td><input type="email" id="email" name="email" value="<%= student.email %>" required></td>
        </tr>
        <tr>
          <td colspan="2"><button style="background-color:peachpuff;"  type="submit">Update Student</button></td>
        </tr>
      </form>
    </tbody>
  </table>
  <h3><p><b><a style="color:green"  href="/students">Back to Homepage</a></b></p></h3>
</body>
</html>
